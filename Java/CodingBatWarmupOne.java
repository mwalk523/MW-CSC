
public class CodingBatWarmupOne

{
   public boolean sleepIn(boolean weekday, boolean vacation) //weekday and vacation are the parameters
   /*A method header like public static void, public is the visibility, sleepIn is the method name (replacing main), 
   and boolean is the return type (replacing void) */
    
   {
      if (weekend) 
      {
         return true;
      }
      else if (vacation)
      {
         return true;
      }
      else 
      {
         return false:
      }  
      
      public int example(int age, int rank, int serialNumber)
      {
         if (age > 17 || rank > 5)
         {
            return serialNumber;
         }
         return Integer.MIN_INT;          
   }

}